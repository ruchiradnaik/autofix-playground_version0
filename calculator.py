# Verified by CodeSentinel
from utils import add_numbers  # Importing the add_numbers function from the utils module

# Verified by CodeSentinel
def run_calculation():
    val1 = 10  # First value for addition
    val2 = 5   # Second value for addition
    
    # Using the add_numbers function to perform the addition
    result = add_numbers(val1, val2)  # Corrected the function call to add_numbers
    
    # Printing the result of the addition
    print(f"The sum of {val1} and {val2} is: {result}")

# Verified by CodeSentinel
if __name__ == "__main__":
    run_calculation()  # Running the calculation when the script is executed

# CodeSentinal: created for you by RuchirAdnaik.